<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!--Appbar背景色-->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!--状态栏颜色-->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!--控制各个控件被选中时的颜色-->
        <item name="colorAccent">@color/black_text</item>
        <!--toolbar title字体颜色-->
        <item name="android:textColorPrimary">@color/black_text</item>
        <item name="colorControlNormal">@color/black</item>
        <item name="actionOverflowMenuStyle">@style/AppTheme.PopupOverlay</item>
        <item name="actionMenuTextAppearance">@style/MenuTextStyle</item>
        <item name="alertDialogTheme">@style/AlertDialogCustom</item>
    </style>

    <style name="MenuTextStyle">
        <item name="android:textColor">@color/black_text</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="AlertDialogCustom" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- NeutralButtonStyle -->
        <item name="buttonBarNeutralButtonStyle">@style/buttonBar</item>
        <!-- NegativeButtonStyle -->
        <item name="buttonBarNegativeButtonStyle">@style/buttonBar</item>

        <item name="buttonBarPositiveButtonStyle">@style/buttonBar</item>
        <!-- PositiveButtonStyle -->
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <style name="buttonBar" parent="@style/Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">@color/black_text</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="Widget.AppCompat.Light.PopupMenu.Overflow">
        <item name="overlapAnchor">false</item>
        <item name="android:popupBackground">?attr/colorPrimary</item>
        <item name="android:dropDownVerticalOffset">5dip</item>
    </style>

</resources>
